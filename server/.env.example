# Server Configuration
PORT=4000

# MongoDB Configuration (Atlas URL)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/realtime-collab?retryWrites=true&w=majority

# Auth0 Configuration
AUTH0_AUDIENCE=https://dummy-api.com
AUTH0_ISSUER_URL=https://dummy-domain.auth0.com/

# CORS (comma-separated, no trailing slashes)
CLIENT_URL=http://localhost:3000,https://your-orbit-app.vercel.app

# Optional Email Settings (Nodemailer / Gmail)

